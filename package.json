{
  "name": "fast-track-zepp",
  "version": "2.2.0",
  "private": true,
  "description": "Premium fasting timer and Flame streak tracker for Amazfit watches.",
  "scripts": {
    "dev": "zeus dev",
    "preview": "zeus preview",
    "build": "zeus build",
    "doctor": "node --version && npm --version && zeus --version && zpm --version"
  },
  "engines": {
    "node": ">=20 <21"
  }
}
